Always read `AGENTS.md`, `.ai/state/architecture-context.md`, and `.ai/state/codex-context.md` before major edits.

- Treat Codex Studio as the authority for repo standards and memory updates.
- Use local repo memory before offering new approaches.
- Keep suggested fixes aligned with stored patterns and decisions.
- Keep the PortableCoder brain as the default route and require explicit /brain allow codex before Codex-backed evaluation.
- If local-model features fail, run `./tools/brain doctor` from the PortableCoder root.
