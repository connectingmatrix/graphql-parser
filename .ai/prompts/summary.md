Capture a concise summary after meaningful changes.

- What changed
- Why it changed
- Which files or behaviors were affected
- What validation was run
- If you implemented a written plan, export that plan markdown to `~/dev/codex-plan/graphql-parser/[PLAN HEADING]-DATE-.md`.
- Preserve the original plan date in frontmatter and file timestamps.
- Record the export path.
